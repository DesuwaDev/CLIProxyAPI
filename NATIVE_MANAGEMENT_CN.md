# CPA 内置管理增强

新增功能直接编译进 CPA，本机 SQLite 保存数据，继续使用原来的端口和管理密钥。
无需 CPAMP、额外容器或额外服务。

## 启用

在现有 `config.yaml` 中增加以下配置，然后重启 CPA：

```yaml
native-management:
  enabled: true
  database: "data/native-management.sqlite"
  retention-days: 90
  modules:
    history: true
    pricing: true
    accounts: true
    diagnostics: true
    limits: true
    fingerprint: true
    headers: true # 仅启用管理能力；每个账号的请求头模式默认关闭
    inventory: true
    risk: true # 仅启用管理能力；风控运行模式默认仍为关闭
```

数据库相对路径以配置文件所在目录为基准。需要已经配置 CPA 管理密钥。
访问原来的 `/management.html`：原版 React 面板直接增加统计和管理功能，
沿用原版登录、导航、主题及凭据记忆选项，没有第二套面板。
原有配置、OAuth、认证文件、配额和插件管理仍在同一界面内使用。
不支持 Home 托管模式；本增强面向独立运行的 CPA。

## 已提供

- SQLite 请求历史，按时间、模型、账号、调用 Key、Request ID、成功/失败和耗时筛选；支持慢请求排序、自定义时段、模型建议及记忆显示列。
- 调用趋势、Token、缓存与推理分类、成功尝试的 P50/P95/P99 与耗时分布；按完整所选时段计算 RPM/TPM，按渠道/模型/账号/Key 聚合。
- 请求诊断：关联客户端最终结果与各次上游尝试，识别流式 HTTP 200 中的错误；查看实际采集到的服务等级、推理强度及上游请求 ID。
- 模型价格规则、服务档位与长上下文阈值、请求发生时的成本快照；可选价格目录同步、本地缓存及手工规则优先覆盖。
- API Key 标签、稳定游标分页、带进度且可取消的 JSONL / Excel 导出（最多 5 万条或 64 MiB）。
- 当前账号运行状态、每 15 分钟及手动记录的状态历史。
- 凭据异常处理队列，可标记待处理、已处理或忽略。
- 独立模块开关，立即生效，重启后保留；关闭模块不会删除历史数据。

次数代表**上游尝试**，包含重试，不等于用户发送的请求数。
价格可在“价格规则”中明确配置，也可手动同步 LiteLLM 格式价格目录；自动刷新默认关闭。
目录按实际模型名及响应返回的服务等级匹配，支持 OpenAI fast／priority 同档、flex／batch 与动态长上下文阶梯（含 272k）。Claude 按实际 usage.speed 及已观测的 5 分钟／1 小时缓存写入用量计价。未知格式、缺失档位价格、缓存时长不明确或 Token 分类不完整时保持未计价。
价格修改只影响后续请求，不会悄悄改变历史金额。费用为估算值。

## 逐 Key／账号文件限流与指纹

- 原版“配置面板”的调用 Key 编辑窗口、原版“AI 提供商”的各 Key 编辑区，以及原版“认证文件详情 / 编辑”内，都有“用量资料、访问策略与请求设置”折叠区；没有新增第二套面板。
- RPM 和并发数分别配置，0 为不限；这里的策略单独保存，点击“应用限流设置”立即生效。新建或更换上游 Key 后先保存，再重新打开编辑窗口设置策略。
- 调用 Key 的 RPM 按滚动 60 秒获准请求计数，并发覆盖整个请求；WebSocket 连接占一个调用 Key 名额。上游凭据按每次实际尝试计 RPM（含重试），流式结束才释放并发。
- 本地繁忙时跳过该凭据，允许尝试其他账号；全部繁忙返回 429，不把账号标记为额度耗尽，不触发异常冷却。计数是单进程的，重启清空；策略持久保存，多实例不共享计数。
- Codex OAuth 账号新增四种指纹收敛模式：默认关闭、仅设备、会话收敛（隔离调用者线程）、完整收敛（共享线程）。完整模式的并发对话共享标识，需要隔离时选择会话模式。
- 每个账号使用持久的随机种子，刷新凭据、切换开关或重启不会自动更换。HTTP、compact、WebSocket 的身份字段在最终出站时统一，启用时优先于原有全局身份改写。
- WebSocket 模式切换会处理旧握手身份，沿用原有重连／重放机制。保留显式自定义缓存 Key、工具标识等无关字段。这是请求身份收敛，不是 TLS 指纹伪装，也不保证缓存命中率。
- 更换 Key、上游地址或账号文件名可能产生新凭据标识；策略不自动迁移到不同账号，以免误用。

## 请求头规范化

在原版“认证文件”的 Codex OAuth 账号编辑窗口展开“用量资料、访问策略与请求设置”，可单独保存请求头模式：

- **关闭**：默认状态，沿用 CPA 原有处理。
- **仅清理代理头**：清理 Forwarded、X-Forwarded-*、Via、代理 IP/CDN 元数据、连接专用头及旧的正文长度／编码声明。认证、账号和已配置的上游 Cookie 保持由选中账号提供，HTTP/WS 传输层生成实际长度和握手字段。
- **保留客户端身份并清理**：在清理基础上，恢复客户端实际送入且有效的 UA / Originator 完整组合；Version 有则保留，没有则不凭空补充。缺少完整组合或字段重复、无效时，沿用执行器默认身份。启用时此模式优先于 CPA 默认 cloaking 和这三个身份字段的自定义头配置。

不会透传调用端 Authorization 或 Cookie；X-Codex-Routing-Hint 只在请求中的账号 ID 与实际选中 OAuth 账号一致时转发，防止把某账号的路由提示用于账号池里的其他账号。不会照搬客户端的 zstd 编码声明到 CPA 重建后的普通 JSON 正文。

账号策略只保存账号索引和模式，不保存请求头值或令牌。可在原版“功能模块”页整体关闭，账号策略独立持久保存。仅对 Codex OAuth 生效，普通 API Key 不显示此选项。

它与指纹收敛分别开关：请求头规范化先处理客户端标识，指纹模块随后处理设备、会话及正文对应字段。只启用请求头模式不会关闭上游原有的身份改写功能。HTTP、compact 和 WebSocket 共用最终出站扩展点；切换策略或稳定握手标识变化时，旧 WS 连接按既有重连／重放流程更新。已经开始的尝试保留当时的策略。

该功能减少代理额外引入的请求头差异，不更改 TLS、模型或推理参数，也不保证不被识别或改善回答质量。当前验证使用本地合成凭据和模拟上游，未向 VPS 部署。

### Codex CLI 版本设置

原版“功能模块”页提供“Codex CLI 版本”：

- 内置版本已更新为 **0.154.0**，与 2026-09-11 查询的 [OpenAI 官方发布记录](https://learn.chatgpt.com/docs/changelog)及 npm latest 一致。这里只改变版本声明，不安装或升级 Codex。
- “自定义 CLI 版本”填写后固定使用该版本；留空使用已同步稳定版，未同步时使用内置值。自动结果不会低于内置版本，手动填写允许固定较旧版本或预发布版本。
- “立即同步稳定版”手动获取版本；“自动同步官方稳定版”默认关闭，开启后每 6 小时检查。仅查询 OpenAI 官方 GitHub 的 openai/codex 发布记录，不发送业务凭据，也不下载或执行软件。同步跳过预发布、草稿和无关组件；断网、限流或返回旧版本时保留原值，手动填写不会被覆盖。
- 页面显示当前生效版本、来源、已同步版本和最近检查时间。设置与缓存重启后保留；关闭自动同步保留缓存，关闭请求头模块停用版本覆盖并停止后台同步。两种关闭操作都会取消正在进行的自动获取。
- UA 前后两处版本和已有 Version 头保持一致；请求没有 Version 头时不补。只处理可识别的 Codex 软件标识，不改正文、模型、系统或终端名称。
- 账号选择“清理代理信息＋沿用客户端标识”且传入标识有效时，客户端版本优先。账号清理模式关闭仍可使用全局版本设置；关闭整个请求头模块才停用这些全局设置。

实现分为 internal/codexidentity（版本格式与声明）、internal/native/headers/versions.go（设置、持久化与同步）和独立前端 CodexVersionEditor，复用已有模块与出站扩展点。参考 sub2api 的稳定版同步与手动优先规则，没有复制其用户系统。当前仍未部署 VPS；VPS 上此前观察到的 0.146.0 不会因本地构建自动改变。

### CLI 版本设置验证与最新构建（2026-09-11）

- 新增 3 项聚焦 Go 测试，覆盖版本声明一致性、稳定版筛选、错误回退、不降级、手动优先、并发修改、持久化和后台取消；复用并更新已有 HTTP / WS 普通与流式出站检查。相关 race 检查通过。
- 前端原有 445 项测试通过；最终 ESLint 无警告，TypeScript 与单文件构建通过。浏览器实际保存手动版本、执行官方同步并核对优先级；重启后手动设置、同步缓存及检查时间保留，没有重复下载。模块关闭返回 409，重新开启恢复设置。
- 最终面板显示当前版本与同步稳定版均为 0.154.0，已核对界面布局和控制台；服务返回的面板与嵌入文件相同。
- 最新 Windows 程序仍为 `bin/cli-proxy-api-native-headers.exe`，SHA-256：`f67d2dc8add44904e575009c6f1fc84673526049a5415ccc3303583549454949`；面板 SHA-256：`c9fcb07fcdbfa9d0a36d79a133be78269af99a227cd263be74d9e35838a4a3a4`。
- 记录位于 `temp/native-version-smoke/verification.json`、`before-restart.json`、`after-restart.json` 及检查日志。出站验证使用本地执行器模拟服务；单独账号文件探针的 base_url 未被原版加载器采用，其外部 TCP 连接失败，未完成 HTTP 发送。未使用真实业务凭据、未部署 VPS。

### 较早的请求头模块验证与构建（2026-09-11）

- Go 内置模块与出站扩展包的完整 race 检查通过；针对性验证覆盖 HTTP / WebSocket 普通与流式请求、账号边界、重复或无效输入、指纹组合和握手身份变化。
- 前端 445 项测试、ESLint、TypeScript 与单文件构建通过。浏览器验证原版账号编辑窗口保存策略、二进制重启后保留，以及功能模块关闭 / 重开；关闭时策略接口返回 409，重开后恢复已存模式。
- 浏览器实际核对新控件布局，未发现控制台错误或警告。服务返回的面板与嵌入文件 SHA-256 一致：`901119d0d6b7810d0c91c6c7cbdce459c5a2a517e91b97b16a75b5bd5febcc58`。
- 本次单独生成 `bin/cli-proxy-api-native-headers.exe`，没有覆盖正在运行的旧 `cli-proxy-api-native.exe`。新文件 SHA-256：`5029ee07e179c7b589182950b950cf676c0001ef1fd29b6618d1548b09db8968`。
- 本地合成验证记录：`temp/native-headers-smoke/verification.json`；前端检查日志：`temp/native-headers-smoke/frontend-verify.log`。

后续仅调整前端说明：请求头选项改为“清理代理信息”“清理代理信息＋沿用客户端标识”；指纹选项直接说明是否统一设备、会话和对话编号。四种语言同步更新，按用户要求未重新运行测试，仅完成前端及 Go 打包。更新后的 `bin/cli-proxy-api-native-headers.exe` SHA-256 为 `8977f3c092ddf62e1bffe79a80248e4053348ac300c5c4fce608b54eb26007e2`，嵌入面板为 `ae537640a3dec0faa951bd0bb3f9616419179caac356dd9ab29fc8db4c014534`；上面的浏览器验证记录对应文案调整前构建。

## 联网价格

“价格规则”可选择 LiteLLM、sub2api 价格源或自定义同格式 JSON，保存后手动同步，也可开启定时刷新（默认关闭）。网络失败时沿用最后成功的目录。页面显示来源、同步时间，并提供精确模型名查询表，列出各服务等级、上下文起点、输入、输出、缓存读写及一小时缓存单价。

不固定套用“fast 两倍”或“超过 200k 翻倍”等倍率；以目录明确提供的各档价格为准。某服务档在长上下文缺少价格时显示未知，实际降到普通档的响应按普通档估算。来源可能滞后或互相冲突，手工规则仍可覆盖；历史请求金额保持原快照。

核对参考：[OpenAI Fast mode](https://openai.com/api-fast-mode/)、[Claude Fast mode](https://platform.claude.com/docs/en/build-with-claude/fast-mode)。不会调用供应商账单或引入 sub2api 的用户体系。

## 账号维护边界

账号状态基于 CPA 已有运行时观测，不会额外发起模型请求。
没有观测数据时显示未知，不会假装已经通过外部健康检查。
当前提供的是记录和人工处理功能，**不会自动禁用、恢复或删除账号**；
核心原有的额度冷却、重试和恢复逻辑继续工作。
没有引入 sub2api 的用户、充值、订阅、分销等账号系统。主动额度探测和 CPAMP 数据库迁移未包含在此版本中。

## 诊断与统计口径

- 请求历史计上游尝试，请求诊断计客户端 POST 请求；鉴权失败可能没有上游尝试，重试可能产生多次尝试。
- 关联使用独立 UUID，不改变 CPA 原有 Request ID。旧记录没有关联信息时明确显示不可用。
- 流式检查只暂时解析不超过 64 KiB 的单行数据，识别支持的错误帧，不保存正文。超长或无法解析的数据标记为检查不完整；HTTP 正常结束不代表模型必然成功。
- 诊断覆盖 `/v1`、`/v1beta`、`/openai/v1`、`/backend-api/codex` 下的 POST，跳过 WebSocket；可独立关闭。
- 耗时分位数使用成功且有实测值的样本，首字延迟只计流式样本；没有采集的值保持未知，不补零。
- 导出按首次查询时的记录范围固定分页；之后到达的记录不混入。若导出期间记录被保留期清理，清理后的记录不再可导出。
- 价格源刷新失败或取消时保留上次成功目录，后续估算继续使用缓存；历史费用不重算。

## 开关、数据与维护

- 总开关、数据库路径和保留期修改后需要重启；页面模块开关立即生效。
- YAML 中的模块配置为新数据库的初始值，之后以页面保存的开关为准。
- 原版 `usage-statistics-enabled` 控制外部用量队列；本增强直接接收进程内事件，互不消费对方的数据。
- 默认保存 90 天，过期记录每 15 分钟分批清理；不会自动压缩数据库文件。
- 备份前先正常停止 CPA，再复制数据库；如果有 `-wal` / `-shm` 文件，一并保留。
- 正常退出会等待用量事件写完；强制杀进程可能丢失尚未落盘的内存队列。
- 磁盘写入失败会记录日志，并在控制台显示失败次数；此时统计可能不完整。
- 诊断使用独立的 1024 条有界写入队列；拥塞丢失或写入失败会在诊断页／模块页明确显示计数，不阻塞代理请求。
- 原始 Key、OAuth 元数据和上游错误正文不会进入统计数据库。
- 数据库自动进行模块内事务迁移；本版历史结构升级至 v2，保留旧记录和费用。升级前可按上述方式备份；旧二进制不能读取新结构，回退需恢复升级前备份。

后端源码集中在 `internal/native/`；原版面板源码在 `management-ui/`，
新增页面集中在 `src/features/nativeManagement/`，各功能通过注册表接入导航和路由。
前端编译为单个 HTML，再嵌入 CPA 二进制，无需独立 Web 服务。
前端改动必须先执行构建脚本；单独 `go build` 使用已生成的面板。
[原版前端版本及同步指南](management-ui/UPSTREAM.md)。
[模块边界、管理 API、同步上游与移除指南](internal/native/README.md)。

## 构建与验证

```powershell
go build -o bin/cli-proxy-api-native.exe ./cmd/server
go test ./internal/native/... ./sdk/cliproxy/usage
go test ./internal/api -run TestNativeManagementUsesExistingAuthorization
go test -race ./internal/native/... ./sdk/cliproxy/usage
./scripts/build-native.ps1
# 前端修改后，在 management-ui 目录执行：
npm exec --yes --package=bun@1.3.14 -- bun run verify
```

新增验证集中在持久化、统计口径、稳定分页、流式错误、价格回退、导出和并发退出，不追求大量重复测试。

## 前一轮统计增强验证（2026-09-11）

- 原版前端检查全部通过：444 项既有及新增测试、ESLint、TypeScript 与单文件构建；增强功能累计新增测试仅 3 项。
- Go 模块测试、管理鉴权测试及并发竞态检查通过，已生成 `bin/cli-proxy-api-native.exe`。
- 本地模拟上游验证了普通响应、流式响应、凭据错误及 HTTP 200 流中错误；升级保留原有 3 条记录与 0.0054 美元快照，新增后为 7 次上游尝试、5 条客户端诊断，关联、分页与价格缓存通过，写入失败为 0。
- 再次重启后 7 次尝试、5 条诊断及 2 个测试价格模型完整保留；Excel 文件通过独立 ZIP 校验和 XML 解析，浏览器验证取消导出后可正常继续操作。
- 浏览器验证了同一次登录下的原版认证文件/配额/配置，以及新增的 7 个页面；本轮重点检查了慢请求筛选、显示列、真实 Excel 导出、请求关联、价格目录独立保存和诊断模块开关。
- 深浅主题正常，约 502px 的窄视口无页面横向溢出；浏览器控制台未发现错误。
- 本轮新增 3 项 Go 回归测试和 1 项前端回归测试；其余测试沿用已有检查。

本轮增加限流、指纹和阶梯价格验证，最终构建与校验值见下节。

这些检查覆盖本次改动的关键路径，不代表对全部供应商与全部运行环境作出零缺陷保证。


## 本轮限流、指纹与价格增强验证（2026-09-11）

- 本轮新增 6 项聚焦的 Go 回归测试，覆盖滚动窗口、并发竞争、即时修改、流式释放、跳过繁忙凭据、账号种子持久化、调用者线程隔离、实际响应档位、272k 边界、5 分钟／1 小时缓存分档及 HTTP/WS 出站。
- 原有调度包和用量解析辅助包测试、Codex 相关测试、管理鉴权检查通过；最终相关模块的 `go test -race` 通过。
- 前端 444 项测试、ESLint、TypeScript 和单文件构建通过。浏览器验证原版调用 Key 编辑、上游 Key 编辑及认证文件详情中的内置控件，确认普通 API Key 不显示 OAuth 指纹选项。
- 实际 CPA 接口对独立调用 Key 和上游凭据均验证首次 200、第二次 429；返回 Retry-After，执行完成后 active 为 0，凭据未被禁用或标记额度冷却。
- 原版认证文件编辑窗口保存 RPM=12、并发=2、会话收敛；完整二进制重启后策略及模式保留，运行计数归零。不同账号、不同调用者和持久种子的行为另由模块测试验证。
- 真实 HTTPS 下载 LiteLLM 目录成功：本次快照载入 3,236 个模型，跳过 653 条非支持记录。实际查询了 gpt-5.6-sol 的普通／fast／flex 档及 272,001 起点；没有提供的 batch 长上下文价格显示未知。目录缓存重启后保留。
- 浏览器核对最终模型价格表，表头不再出现缺失翻译键；窄视口下表格在自身容器滚动，页面未横向溢出。
- 价格目录版本升级为 v2，仅增加格式版本屏障，保留旧目录数据；旧版本二进制不能把新档位数据误读为标准价。升级前应备份数据库。

最终 Windows 构建：`bin/cli-proxy-api-native.exe`。
该轮构建 SHA-256：`92d501a502bfd7162b33bb7ad0a56391f82a82a7048e756fb077ba5523f5f6e7`；已被下述风控与用量增强构建替代。
验证记录位于工作区 `temp/native-controls-*.log` 与 `temp/native-smoke/*verification*.json`，仅使用本地合成凭据；未使用真实 OAuth 账号测试外部供应商端到端响应。


## 请求风控、账号用量与 Key 资料（2026-09-11）

### 请求风控

原版导航直接显示“请求风控”，包含概览、策略、审查节点、事件、拦截名单和测试六个页签。新安装默认关闭运行模式；模块开关与运行模式分开保存。

- 关闭：按原有流程转发；观察：异步审查并记录，不拦截；发送前拦截：先完成审查，再允许业务上游执行。
- 可选择本地关键词 / RE2 正则、模型审查或两者组合；选择渠道、模型包含 / 排除范围，支持全上下文或最近一轮（含后续工具调用与结果）。默认只针对 Codex。
- 审查节点支持标准 OpenAI 兼容 JSON 审查模型、Qwen3Guard 与 Moderations 协议；节点可排序、独立开关和测试，失败按顺序尝试下一节点。可配置分类分值阈值、并发上限、文本长度及分块大小。
- 提示词、代码、工具定义和参数参与检查；图片不解码、不下载。超限或无法完整检查时记为审查异常，不把截断输入当成通过。模型是否识别风险仍取决于所配置审查模型。
- 拦截模式默认在审查失败时拒绝请求，返回 503；明确命中返回 403。可选择异常放行。普通请求、流式请求及复用会话的后续执行均经过检查，不会用换账号重试绕过本地拒绝，也不影响账号健康状态。
- 可启用已命中内容哈希拦截及会话 TTL 拦截；会话按调用 Key 隔离，名单可查看并手工解除。只有上游结构化错误码 cyber_policy 才触发对应会话记录，错误正文里出现同名字符串不算命中。
- 事件记录分类、分值、规则、审查节点、耗时、策略版本和请求关联；不保存原始代码、提示词、审查回复或明文密钥。允许记录默认关闭。观察队列有界，丢弃及写入失败有计数。
- 审查节点 Token 只写不读，以 AES-GCM 加密保存，密钥文件为数据库路径加 .risk.key。正常停机备份时，应与数据库一起保留；缺失密钥时节点审查失败，不会悄悄清空 Token。

要启用语义审查，需要配置可用的审查模型服务；CPA 不内置模型权重。本地规则可单独运行。审查请求会把所选文本发给配置的审查节点，测试页不会发起业务模型请求。它不能保证预判上游所有 cyber 风控结果。

### 原版账号卡片与 Key 编辑

- 认证文件卡片直接显示累计尝试数、Token、已计价费用、开始记录时间、预算进度及历史入口。可用配额快照出现时显示窗口已用费用、预估总量与剩余量，以及重置 / 观测时间。
- 累计计数独立保存，清理 90 天请求明细不会把累计金额清零。升级时只从仍然保留的请求记录建立基线；开启统计之前、模块关闭期间、进程外使用的用量无法追溯。
- 估算按当前配额窗口内本地已计价费用 / 已使用百分比推算，始终标为部分观测，不等同供应商美元余额。无用量、比例为零、存在未计价请求、快照超过 15 分钟或窗口已过期时显示未知。
- Codex 可复用原版认证文件页配额刷新得到的五小时 / 周 / 月快照，按原始观测时间保存；不把代码审查或 Spark 专用配额套到全账号费用。运行时已观测的 Codex / Claude 窗口也可使用。
- 原版调用 Key、上游 Key 和认证文件编辑区内增加名称、备注、标签、有效期、模型白名单、独立停用及预算提示；原版 Key 列表直接显示名称、备注、标签和累计用量。
- 调用 Key 停用 / 过期直接拒绝；凭据停用 / 过期 / 模型不匹配时可选择其他合格凭据。预算是提醒，不作为精确金额扣款或强制限额；RPM / 并发仍由独立限流模块控制。
- “账号 / Key 用量”提供统一搜索、来源筛选、请求 / Token / 金额排序和资料编辑。名称与请求历史中的 Key 标签同步。

### 观测布局与维护

用量统计采用四张主指标卡、环比变化、次级性能指标、可切换趋势、Token 构成、模型 / Key 排行和最近请求；复杂性能分布收进可展开区域。请求诊断单独统计客户端结果，与包含重试的上游尝试区分。新界面按实际请求时间排序，稳定游标兼容迟到记录。

新增后端分别位于 internal/native/risk 和 internal/native/inventory；前端通过原有模块注册表、API 适配器、认证文件卡片和凭据编辑插槽接入。自动禁用 / 恢复账号仍未启用，没有引入用户、充值、订阅或邮件系统。

参考本地 CPA-Manager-Plus（1ae656c82990c480f3f104326a08c6e0001eeb4c）的信息布局、别名和窗口比例估算，以及 sub2api（98d86915becae9fe9491a91ffc6defd5235c8d2b）的内容审查、Prompt Audit 与会话风控设计；按 CPA 的执行链和模块边界独立实现，未直接复制其服务与账号系统。

### 本轮验证记录

- 新增 8 项聚焦 Go 测试与 1 项前端回归测试；前端总计 445 项通过，ESLint 无警告、TypeScript 与单文件构建通过。
- 内置模块、SDK 认证调度和用量辅助包检查通过；内置模块及认证调度的 race 检查、管理鉴权和 Codex 出站回归通过。账号资料与配额路由冲突、模块识别遗漏、实际请求时间排序问题已在联调中修复并覆盖。
- 本地合成上游验证普通和流式请求命中后立即 403，业务上游新增调用为 0；规则、模型审查、节点失败切换、异常放行 / 拒绝、会话隔离、同一会话后续轮次和模块关闭行为均有覆盖。
- 最终二进制重启后保留 90 次合成账号请求、Key 名称与预算、加密审查节点、风控模式和配额快照；25% 已用窗口对应本地已用 1.7151 USD、预估总額 6.8604 USD、预估剩余 5.1453 USD，均标为部分观测。
- 浏览器已核对原版账号文件卡片的累计值与配额估算、原版编辑器的资料保存、风控模式切换与文本测试；具体截图和交互记录见本次任务工具输出。

本轮最终构建：bin/cli-proxy-api-native.exe。
SHA-256：bafe7ed1b924f1a5f79e13a1ac823175c83f49671d4c7eb5b69fe4a33b52b2ea。
验证日志：temp/native-risk-*-final.log、temp/native-risk-route-race.log；模拟接口结果：temp/native-risk-smoke/verification.json、restart-verification.json、final-verification.json。以上仅使用合成凭据与本地审查节点，未调用真实 OAuth 业务上游。
- 最终原版配置面板已验证 Key 名称、备注、标签和累计用量直接展示；账号编辑器保存后，原版卡片同步更新。浏览器控制台没有发现错误或警告。
- 窄屏实际可用宽度约 501px，账号 / Key 用量表格在自身容器滚动，页面没有横向溢出；桌面 1441px 原版账号卡片的累计值与窗口估算布局正常。
- 最终用量页在约 501px 窄屏下显示两列核心指标，趋势与排行自适应，页面无横向溢出；最近请求显示日期、时间及中文失败状态，按请求发生时间降序排列。
- 深浅主题均已浏览器核对；最终服务实际返回的 management.html 与已验证构建的 SHA-256 完全相同（00cbf4df49eb279783eccce6d5fbb05517b3e890e4d6f8fefa88fc37478b7151），确认运行的是最终嵌入面板。
